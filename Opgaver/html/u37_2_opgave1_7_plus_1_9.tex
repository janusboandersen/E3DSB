
% This LaTeX was auto-generated from MATLAB code.
% To make changes, update the MATLAB code and republish this document.

\documentclass[a4paper]{article}
\usepackage{graphicx}
\usepackage{color}
\usepackage{amsfonts, amsmath, amssymb}
\usepackage{fancyhdr}
\usepackage{polyglossia}
\setdefaultlanguage{danish}
\usepackage[binary-units=true]{siunitx}
\sisetup{detect-all}

\pagestyle{fancy}
\fancyhead{}
\fancyfoot{}
\fancyhead[L]{\slshape{\MakeUppercase{Differential BJT amplifiers}}}
\fancyhead[R]{\slshape{Janus Bo Andersen}}
\fancyfoot[C]{\thepage}


\sloppy
\definecolor{lightgray}{gray}{0.5}
\setlength{\parindent}{0pt}

\begin{document}

    
    
\subsection*{Contents}

\begin{itemize}
\setlength{\itemsep}{-1ex}
   \item Opgave 1.7
   \item Opgave 1.9
\end{itemize}


\subsection*{Opgave 1.7}

\begin{par}
Eksponentielt aftagende signal - "ding-lyd" KPL 2019-09-10
\end{par} \vspace{1em}
\begin{verbatim}
clear; close all

fs = 44100;   % Hz
f0 = 440;     % Hz
Tdur = 2;     % varighed ("duration") 2 sekunder
N  = Tdur*fs; % samples svarende til 2 sekunder
n  = 0:N-1;
t  = n/fs;

% Firkantsignal med første tre harmoniske (kun de ulige harm.):
x =     sin(2*pi*1*f0/fs*n) + ...
    1/3*sin(2*pi*3*f0/fs*n) + ...
    1/5*sin(2*pi*5*f0/fs*n);

% Envelope
env = exp(-n*5/N);

xe = x.*env;

figure
plot(t,xe,...
     t,env,...
     t,-env,...
     'LineWidth',2)
xlim([0 Tdur])
legend('Eksponentielt aftagende signal','Envelope','-Envelope',...
       'location','NorthEast')
xlabel('t [s]')

sound(xe,fs)

audiowrite('expding.wav',xe,fs)
\end{verbatim}

\includegraphics [width=4in]{u37_2_opgave1_7_plus_1_9_01.eps}


\subsection*{Opgave 1.9}

\begin{par}
Benytter LFO til at lave vibrato Lav et 5 Hz square wave
\end{par} \vspace{1em}
\begin{verbatim}
LFO = square(2*pi*5*t);

% Paatryk LFO på oprindeligt signal
z = x.*LFO;
sound(z, fs)
\end{verbatim}



\end{document}

